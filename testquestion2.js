let pokemons = ["one", "two", "three"];

function setup() {
    createCanvas(200, 300);
    background(100);
    textSize(20);
    textAlign(CENTER, CENTER);
    fill("pink");
    text("hi", width/2, 50);
}